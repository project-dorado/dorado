<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Dorado.Plugins.LastFm</RootNamespace>
    <AssemblyName>Dorado.Plugins.LastFm</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Dorado.Plugins.Sdk\Dorado.Plugins.Sdk.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Update="plugin.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <!-- `dotnet publish` also emits a distributable .znp package next to the output. -->
  <Target Name="PackZnp" AfterTargets="Publish">
    <PropertyGroup>
      <ZnpDestination>$(PublishDir)../$(AssemblyName).znp</ZnpDestination>
    </PropertyGroup>
    <ZipDirectory SourceDirectory="$(PublishDir)" DestinationFile="$(ZnpDestination)" Overwrite="true" />
    <Message Importance="high" Text="Packed plugin: $(ZnpDestination)" />
  </Target>

</Project>
